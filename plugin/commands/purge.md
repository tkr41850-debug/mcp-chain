---
description: Purge chain entries (requires <id>, --all, or --resolved)
argument-hint: <id> | --all | --resolved
---

Run `"${CLAUDE_PLUGIN_ROOT}/scripts/mcp-chain-exec.sh" purge $ARGUMENTS` and echo any output verbatim. The binary errors if no argument is given.
