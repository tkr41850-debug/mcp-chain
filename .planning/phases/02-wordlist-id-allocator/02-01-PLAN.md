---
phase: 2
plan: 01
slug: wordlist-id-allocator-core
status: planned
autonomous: true
requirements: [CORE-07]
depends_on: []
wave: 1
files_modified:
  - internal/idgen/eff_short_wordlist_1.txt
  - internal/idgen/wordlist.go
  - internal/idgen/idgen.go
  - internal/idgen/idgen_test.go
  - go.mod
  - go.sum

must_haves:
  truths:
    - "Running `go test ./internal/idgen/...` produces 4 passing tests (TestWordlistInvariants, TestWordlistBoundaries, TestAllocate, TestAllocateMonotonicUniqueOverBoundary) with no panics or failures."
    - "Calling `idgen.Allocate(0)` returns `\"acid\"`, `idgen.Allocate(1295)` returns `\"zoom\"`, and `idgen.Allocate(1296)` returns `\"hex-0001\"` — deterministically, with no filesystem or random dependency."
    - "Calling `idgen.Allocate(66831)` returns `\"hex-10000\"` (5 hex digits) without collision with any earlier ID — `%04x` is a minimum-width specifier and widens automatically."
    - "The committed `internal/idgen/eff_short_wordlist_1.txt` has sha256 `8f5ca830b8bffb6fe39c9736c024a00a6a6411adb3f83a9be8bfeeb6e067ae69` (byte-verified against EFF upstream, pinned so tamper breaks the build)."
    - "Package `init()` panics loudly if the embedded wordlist has != 1296 entries, any duplicate, any word outside `[a-z-]+`, or any `hex-` prefix (tested implicitly by the test binary loading without panic)."
  artifacts:
    - path: "internal/idgen/eff_short_wordlist_1.txt"
      provides: "Raw EFF short wordlist TSV, 1296 lines, LF-terminated, pure ASCII, verbatim upstream"
      contains: "acid"  # first word, EFF dice roll 1111
    - path: "internal/idgen/wordlist.go"
      provides: "//go:embed + parseAndValidate() + init() panic-on-invariant; package doc with CC-BY 4.0 attribution"
      exports: ["(package-private only — words, wordlistSize, parseAndValidate, isValidWord)"]
      min_lines: 60
    - path: "internal/idgen/idgen.go"
      provides: "Allocate(counter uint64) string — pure function covering wordlist lookup + hex fallback"
      exports: ["Allocate"]
      min_lines: 15
    - path: "internal/idgen/idgen_test.go"
      provides: "Table-driven boundary tests + invariant reassertion (CORE-07 coverage)"
      contains: "TestAllocate"
      min_lines: 60
  key_links:
    - from: "internal/idgen/wordlist.go"
      to: "internal/idgen/eff_short_wordlist_1.txt"
      via: "//go:embed directive on the line immediately preceding `var rawWordlist string`"
      pattern: "//go:embed eff_short_wordlist_1\\.txt"
    - from: "internal/idgen/wordlist.go init()"
      to: "parseAndValidate (enforces count, charset, dedup, no hex- prefix; panics on violation)"
      via: "init() { words = parseAndValidate(rawWordlist) }"
      pattern: "words = parseAndValidate"
    - from: "internal/idgen/idgen.go Allocate"
      to: "internal/idgen/wordlist.go words slice"
      via: "index lookup when counter < len(words); fmt.Sprintf fallback otherwise"
      pattern: "words\\[counter\\]"
    - from: "internal/idgen/idgen_test.go"
      to: "github.com/stretchr/testify/require v1.11.1"
      via: "first import in the project; go mod tidy lands it in go.mod/go.sum"
      pattern: "stretchr/testify/require"
---

<objective>
Deliver a pure, deterministic `idgen.Allocate(counter uint64) string` over the embedded EFF short wordlist with a clean zero-padded hex fallback past 1296, fully unit-tested with zero dependency on store, filesystem, or concurrency primitives.

Purpose: Phase 4 (store) needs a stateless, allocation-light function that maps its monotonic counter to a short memorable ID. Ship that function in isolation so the store phase is purely about counter, flock, and schema — with ID generation already verified and test-pinned.

Output: Four files under `internal/idgen/` (TSV data, wordlist.go, idgen.go, idgen_test.go) + `testify/require` lands in go.mod via `go mod tidy`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-wordlist-id-allocator/02-CONTEXT.md
@.planning/phases/02-wordlist-id-allocator/02-RESEARCH.md
@.planning/phases/02-wordlist-id-allocator/02-VALIDATION.md
@go.mod
@.golangci.yml

<interfaces>
<!-- No existing code to import from — this is a greenfield package.
     The new public surface (introduced by this plan) is minimal:

From internal/idgen/idgen.go (NEW):
```go
// Allocate returns a deterministic ID for the given monotonic counter.
//   counter < 1296            → the EFF wordlist entry at index counter
//   counter >= 1296           → fmt.Sprintf("hex-%04x", counter-1295)
// Pure, safe for concurrent use, no error path.
func Allocate(counter uint64) string
```

From internal/idgen/wordlist.go (NEW, package-private):
```go
const wordlistSize = 1296        // package-private per Open Question #2
var rawWordlist string           // //go:embed target
var words []string               // populated in init(); immutable after
```

No upstream callers in Phase 2 — Phase 4's store will be the first consumer of `Allocate`.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download & commit the EFF short wordlist (sha256-pinned)</name>
  <files>internal/idgen/eff_short_wordlist_1.txt</files>
  <action>
Download the EFF short wordlist verbatim from the canonical URL and commit it into the repo under `internal/idgen/` with a cryptographic integrity check. Do NOT reformat, strip columns, re-encode, or edit the file in any way — the sha256 pin is the provenance anchor.

Steps:

1. Create the package directory if missing:
   ```
   mkdir -p internal/idgen
   ```

2. Download with curl (fail-fast flags matter — without `-f`, curl exits 0 even on HTTP 404):
   ```
   curl -sSfLo internal/idgen/eff_short_wordlist_1.txt \
     https://www.eff.org/files/2016/09/08/eff_short_wordlist_1.txt
   ```

3. Verify the sha256 matches the pinned value (see RESEARCH.md lines 616, 647 and the Wordlist file commit section at lines 613–618). On Linux use `sha256sum`; on macOS fall back to `shasum -a 256`:
   ```
   echo "8f5ca830b8bffb6fe39c9736c024a00a6a6411adb3f83a9be8bfeeb6e067ae69  internal/idgen/eff_short_wordlist_1.txt" \
     | sha256sum -c -
   ```
   If the check fails, DELETE the file and stop — do not attempt to proceed with a tampered wordlist. The whole point of the pin is that a hash mismatch fails the build before it can land.

4. Sanity-check format (belt-and-braces; the parse-time invariants in Task 2 will re-verify at build time):
   - `wc -l internal/idgen/eff_short_wordlist_1.txt` → `1296`
   - `wc -c internal/idgen/eff_short_wordlist_1.txt` → `13660`
   - First line: `1111\tacid`, last line: `6666\tzoom` (tab-separated; dice roll, then word)

5. Audit `.gitignore` to confirm the new file is NOT silently ignored. The current `.gitignore` at repo root ignores `/mcp-chain`, `/mcp-chain.exe`, `/dist/`, `*.out`, `coverage.*`, `.idea/`, `.vscode/`, `*.swp`, `.DS_Store` — none of these patterns touch `internal/idgen/*.txt`, so the file will stage cleanly. Run `git check-ignore -v internal/idgen/eff_short_wordlist_1.txt`; it MUST exit non-zero (nothing ignoring it). If it exits 0, report which rule matched and STOP — do not add overrides without investigating.

Commit message: `feat(02-01): task 1: add EFF short wordlist (CC-BY 4.0, sha256-pinned)`

Commit body should mention: source URL, sha256, CC-BY 4.0 license (attribution lands in wordlist.go in Task 2; a top-level NOTICE file is deferred to Phase 10 per RESEARCH.md Assumption A3 and Open Question 1).
  </action>
  <verify>
    <automated>test "$(sha256sum internal/idgen/eff_short_wordlist_1.txt | awk '{print $1}')" = "8f5ca830b8bffb6fe39c9736c024a00a6a6411adb3f83a9be8bfeeb6e067ae69" &amp;&amp; test "$(wc -l &lt; internal/idgen/eff_short_wordlist_1.txt)" = "1296" &amp;&amp; ! git check-ignore internal/idgen/eff_short_wordlist_1.txt</automated>
  </verify>
  <done>
- `internal/idgen/eff_short_wordlist_1.txt` exists, 13,660 bytes, 1296 lines, sha256 matches the pinned value.
- `git check-ignore` confirms the file is NOT excluded by any .gitignore rule.
- File is staged and committed with the specified message.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Embed wordlist + parseAndValidate + invariant tests</name>
  <files>internal/idgen/wordlist.go, internal/idgen/idgen_test.go</files>
  <behavior>
    - Test 1 (TestWordlistInvariants): `words` has exactly 1296 entries; every entry is non-empty, matches `[a-z-]+`, and is unique; no entry begins with `hex-`. (Re-asserts at test time what init() already panics on.)
    - Test 2 (TestWordlistBoundaries): `words[0] == "acid"`, `words[1295] == "zoom"` — pins against accidental file reorder or off-by-one in the parser.
    - Implicit: If `init()` panics (bad count, bad charset, dup, hex- prefix collision, missing tab, malformed embed) the test binary fails to load, surfacing the panic message in the `go test` output — this is the build-break-loudly channel.
  </behavior>
  <action>
Create `internal/idgen/wordlist.go` and `internal/idgen/idgen_test.go`. Both files use copy-paste-ready code from RESEARCH.md — no interpretation required.

**File 1: `internal/idgen/wordlist.go`** — copy the complete block from `02-RESEARCH.md` lines 414–501 ("`wordlist.go` (complete, copy-paste-ready)"). Key requirements embedded in that block (do NOT drift from them):

1. Package doc comment at top includes the EFF upstream URL and explicit **CC-BY 4.0** attribution. This is the Phase 2 placement per RESEARCH.md Open Question 1 resolution (lines 674–678) — a top-level `NOTICE` file lands in Phase 10. Do not omit this comment; it is the sole attribution artifact until Phase 10.
2. Imports: `_ "embed"` (blank-import — `//go:embed` is a compiler directive, not a function call, so the package would otherwise fail `unused-import`; see RESEARCH.md Pitfall 7, lines 395–402), `"fmt"`, `"strings"`. NO other imports.
3. `const wordlistSize = 1296` — package-private per Open Question 2 (RESEARCH.md lines 680–682). YAGNI; Phase 4 can export later non-breakingly.
4. The `//go:embed eff_short_wordlist_1.txt` directive MUST be on the line immediately preceding `var rawWordlist string` with NO intervening blank line or comment — see RESEARCH.md Pitfall 6, lines 381–392.
5. `parseAndValidate` uses `strings.TrimRight(raw, "\n")` BEFORE `strings.Split` — see Pitfall 4 (lines 358–366). Without the trim, the trailing LF produces a 1297th empty line and init() panics.
6. `isValidWord` accepts `[a-z-]+` — the hyphen is needed for exactly one entry (`yo-yo` at line 1286 of the TSV). See Pitfall 1 (lines 324–334). Do NOT restrict to `[a-z]+`.
7. The parse loop checks, in order: tab separator present, word non-empty, charset valid, no `hex-` prefix, no duplicate. Every violation panics with a message that identifies the offending line number and content so a build break is debuggable from `go test` output alone.

**File 2: `internal/idgen/idgen_test.go`** — for this task, create the file containing ONLY the first two tests from RESEARCH.md (Task 3 appends two more). Copy from `02-RESEARCH.md`:

- `package idgen` declaration + imports (`"strings"`, `"testing"`, `"github.com/stretchr/testify/require"`) from lines 533–541.
- `TestWordlistInvariants` from lines 547–561 — re-asserts count / charset / dedup / no-hex-prefix at test time.
- `TestWordlistBoundaries` from lines 565–568 — pins `words[0] == "acid"` and `words[wordlistSize-1] == "zoom"`.

Do NOT yet add `TestAllocate` or `TestAllocateMonotonicUniqueOverBoundary` — those come in Task 3 together with `idgen.go`. Keeping them separated lets Task 2's commit be a self-contained "wordlist embed works" checkpoint.

**After writing:** run `go mod tidy` to land `github.com/stretchr/testify v1.11.1` in `go.mod`/`go.sum` (first import in the project — the module resolver will fetch transitively). Then run `go test ./internal/idgen/...` — both tests must pass.

**Conformance checks to run before commit:**
- `go vet ./internal/idgen/...` — clean.
- `make lint` (or the equivalent `golangci-lint run ./internal/idgen/...`) — clean. `forbidigo` bans `fmt.Print*` + `net/http` but allows `fmt.Sprintf` (used in panic messages) and `fmt.Errorf`, so this code is in-bounds.

Commit message: `feat(02-01): task 2: embed EFF wordlist + parse-time invariants`
  </action>
  <verify>
    <automated>go mod tidy &amp;&amp; go test ./internal/idgen/ -run "TestWordlistInvariants|TestWordlistBoundaries" -v &amp;&amp; go vet ./internal/idgen/...</automated>
  </verify>
  <done>
- `internal/idgen/wordlist.go` compiles with `//go:embed` wired correctly (rawWordlist is non-empty at init).
- CC-BY 4.0 attribution block present in the package doc comment with the EFF URL.
- `TestWordlistInvariants` and `TestWordlistBoundaries` pass.
- `go vet ./internal/idgen/...` and `golangci-lint` clean.
- `go.mod` / `go.sum` updated with `github.com/stretchr/testify v1.11.1`.
- Committed atomically with the specified message.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Allocate + boundary + monotonicity tests</name>
  <files>internal/idgen/idgen.go, internal/idgen/idgen_test.go</files>
  <behavior>
    - TestAllocate (table-driven): counter 0 → "acid"; counter 1 → "acorn"; counter 1295 → "zoom"; counter 1296 → "hex-0001"; counter 1297 → "hex-0002"; counter 66829 → "hex-fffe" (offset 65534); counter 66830 → "hex-ffff" (offset 65535 — 4-digit max); counter 66831 → "hex-10000" (offset 65536; min-width widens automatically to 5 digits); counter 66832 → "hex-10001".
    - TestAllocateMonotonicUniqueOverBoundary: no two counters in [1290, 1310] produce the same string — catches any off-by-one at the wordlist→hex handoff.
    - Allocate is pure: no rand, no mutex, no state mutation; `uint64` input, `string` output, no error.
  </behavior>
  <action>
Create `internal/idgen/idgen.go` and extend `internal/idgen/idgen_test.go` with the two remaining tests.

**File 1: `internal/idgen/idgen.go`** — copy the complete block from `02-RESEARCH.md` lines 505–529 ("`idgen.go` (complete, copy-paste-ready)"). Critical points:

1. Import only `"fmt"`. Nothing else — no rand, no sync, no other packages.
2. The fallback subtraction is `counter - uint64(len(words)-1)`, NOT `counter - uint64(len(words))`. This gives `counter=1296 → hex-0001` rather than the off-by-one `hex-0000`. See RESEARCH.md Pitfall 3 (lines 349–356). The tests in this task pin this exact boundary; the off-by-one would fail `TestAllocate` case "first fallback (hex-0001)".
3. Format string is `"hex-%04x"` (minimum-width, not truncation — RESEARCH.md Pitfall 2 lines 336–346). Do NOT add manual width logic, do NOT slice the result, do NOT switch format strings past 65535. The Go fmt package widens automatically past 4 digits.
4. Branch: `if counter < uint64(len(words))` — using `len(words)` (runtime) rather than the `wordlistSize` const keeps `Allocate` correct even if someone ever adjusts the const without rebuilding the TSV (which the parse invariants would panic on anyway, but belt-and-braces).
5. Godoc on `Allocate` documents the three ranges (wordlist / 4-digit hex / widened hex) and explicitly notes `Requirement: CORE-07`. Retain that comment verbatim — it's the audit trail for REQUIREMENTS.md.

**File 2: append to `internal/idgen/idgen_test.go`** — add at the END of the existing test file (after `TestWordlistBoundaries`):

- `TestAllocate` from RESEARCH.md lines 573–596. Table-driven with these exact counters: 0, 1, 1295, 1296, 1297, 66829, 66830, 66831, 66832. The 66830→"hex-ffff" and 66831→"hex-10000" pair pins that `%04x` widens correctly past 0xffff without alias, per RESEARCH.md Pitfall 2.
- `TestAllocateMonotonicUniqueOverBoundary` from lines 601–610. Asserts no two counters in `[1290, 1310]` produce the same string. This is the handoff test — it catches any off-by-one at the wordlist→hex boundary that the table-driven test might miss.

Do NOT remove or reorder the two tests added in Task 2.

**Conformance checks before commit:**
- `go test ./internal/idgen/ -v` — all four tests green (TestWordlistInvariants, TestWordlistBoundaries, TestAllocate with 10 subtests, TestAllocateMonotonicUniqueOverBoundary).
- `go test -race -count=1 ./...` — full repo race suite green (cheap — `idgen` is pure, and Phase 1 subcommands don't touch anything racy yet).
- `go vet ./...` clean; `make lint` clean.
- `go build ./...` succeeds and the stripped binary still under 15 MB (the embedded wordlist adds ~13.6 KB — well within the 15 MB budget per RESEARCH.md line 69; the Phase 1 size gate remains green).

Commit message: `feat(02-01): task 3: implement Allocate with hex fallback + boundary tests`
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/idgen/... -v &amp;&amp; go vet ./... &amp;&amp; go build ./...</automated>
  </verify>
  <done>
- `internal/idgen/idgen.go` exports `Allocate(counter uint64) string` with the documented three-range behavior.
- All four tests pass under `-race`: TestWordlistInvariants, TestWordlistBoundaries, TestAllocate (10 subtests), TestAllocateMonotonicUniqueOverBoundary.
- `go build ./...` succeeds; stripped binary still within the 15 MB Phase 1 gate.
- `go vet ./...` and `make lint` clean.
- Committed atomically with the specified message.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Upstream EFF file → committed TSV | Third-party content being pulled into the binary; integrity must be verified |
| Committed TSV → running binary | Build-time invariant enforcement (count, charset, dedup, hex- collision) |

Phase 2 has no network, filesystem (runtime), process, or cryptographic surface. `Allocate` is a pure function over a static embedded string. All threats are build-time supply-chain concerns.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `internal/idgen/eff_short_wordlist_1.txt` | mitigate | sha256 pin in Task 1 verify command (`8f5ca830…067ae69`); Task 1 aborts + deletes file on mismatch; any future unreviewed change to the TSV must pass the same pin in CI before merge |
| T-02-02 | Tampering | Supply-chain substitution of wordlist with `hex-`-prefixed words (would silently alias to fallback IDs) | mitigate | `parseAndValidate` in `wordlist.go` explicitly rejects any word starting with `hex-` and panics at `init()` — build breaks before first `Allocate` call |
| T-02-03 | Tampering | Wordlist with injected duplicates (would let two counters produce the same ID, breaking CORE-07 uniqueness) | mitigate | `parseAndValidate` uses `map[string]struct{}` to detect duplicates and panics on any hit; `TestWordlistInvariants` re-asserts uniqueness at test time |
| T-02-04 | Tampering | Wordlist with non-`[a-z-]+` charset (e.g., Unicode homoglyphs indistinguishable from ASCII in some fonts, or whitespace that corrupts parsing) | mitigate | `isValidWord` hard-enforces `[a-z-]+`; panics on any violation with the line number in the message |
| T-02-05 | Information disclosure / Spoofing | ID predictability enabling session hijack | accept | Out of scope for idgen. Session authorization is enforced by the `OwnerToken` (Phase 4 CORE-08), not by ID opacity. IDs are intentionally short and memorable — coordination tokens, not secrets. See RESEARCH.md §Security Domain, Known Threat Patterns table |
| T-02-06 | Denial of service | `uint64` counter overflow | accept | Practically unreachable (2^64 registrations). Documented as "not a defended threat model" — to be re-documented in the Phase 4 store design. |

All mitigations either live in `parseAndValidate` (init-time panics that break the build on violation) or the Task 1 download verify command (sha256 pin as the supply-chain anchor). No runtime defensive code required — `Allocate` itself has no untrusted inputs: every `uint64` is a valid input by definition.
</threat_model>

<verification>
After all three tasks commit, the full phase verification is:

1. **Wordlist integrity:** `sha256sum internal/idgen/eff_short_wordlist_1.txt` prints `8f5ca830b8bffb6fe39c9736c024a00a6a6411adb3f83a9be8bfeeb6e067ae69`.
2. **Embed wiring:** `go build ./...` succeeds; a `strings internal/idgen/ | grep acid` confirms the wordlist is inside the compiled artifacts.
3. **Unit tests green:** `go test ./internal/idgen/ -v` shows 4 top-level tests (TestWordlistInvariants, TestWordlistBoundaries, TestAllocate with 10 table subtests, TestAllocateMonotonicUniqueOverBoundary) all PASS.
4. **Race-safe:** `go test -race -count=1 ./...` passes repo-wide.
5. **Lint + vet clean:** `go vet ./...` + `make lint` both exit 0.
6. **Phase 1 gates preserved:** the stripped `mcp-chain` binary remains under 15 MB and `mcp-chain --version` remains under 100 ms (the 13.6 KB embed and zero-init-work have negligible impact, but the Phase 1 CI gates are the authority).
7. **Dependency hygiene:** `go.mod` lists `github.com/stretchr/testify v1.11.1` (new), `github.com/alecthomas/kong v1.15.0` (from Phase 1), and Go `1.23.8`. No `net/http`, no cgo, no unexpected transitive deps in `go.sum` beyond what testify pulls (davecgh/go-spew, pmezard/go-difflib, gopkg.in/yaml.v3).
</verification>

<success_criteria>
- [ ] `internal/idgen/eff_short_wordlist_1.txt` exists, sha256 matches `8f5ca830b8bffb6fe39c9736c024a00a6a6411adb3f83a9be8bfeeb6e067ae69`, committed verbatim.
- [ ] `internal/idgen/wordlist.go` embeds the TSV via `//go:embed`, parses + validates at `init()` panicking on any invariant violation, and includes a CC-BY 4.0 attribution package doc comment citing the upstream URL.
- [ ] `internal/idgen/idgen.go` exports `Allocate(counter uint64) string` — pure, no error return, branchless hot path (single `if`), no imports beyond `"fmt"`.
- [ ] `internal/idgen/idgen_test.go` contains 4 tests: TestWordlistInvariants, TestWordlistBoundaries, TestAllocate (9 subtests covering counters 0, 1, 1295, 1296, 1297, 66829, 66830, 66831, 66832), TestAllocateMonotonicUniqueOverBoundary.
- [ ] `go test -race -count=1 ./...` green repo-wide.
- [ ] `go vet ./...` + `make lint` exit 0.
- [ ] `go build ./...` produces a binary within Phase 1's 15 MB / 100 ms gates.
- [ ] `go.mod` + `go.sum` updated with `github.com/stretchr/testify v1.11.1`; no cgo, no `net/http`.
- [ ] Three atomic commits on the current branch, one per task, with messages of the form `feat(02-01): task N: <subject>`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-wordlist-id-allocator/02-01-SUMMARY.md` documenting:
- What shipped (four files + go.mod/go.sum delta)
- CORE-07 requirement status: MET (wordlist embed + Allocate deterministic + boundary tests; counter monotonicity + no-decrement-on-purge remain Phase 4's responsibility per REQUIREMENTS.md traceability table)
- sha256 of the committed wordlist (as provenance record)
- `go mod tidy` landed testify v1.11.1 — note the transitive deps added to go.sum
- Phase 1 size / startup gates: still green (record the final stripped binary size)
- **Phase 10 follow-up flagged:** add top-level `NOTICE` / `THIRD_PARTY_LICENSES.md` citing EFF CC-BY 4.0 (currently only attributed in the `wordlist.go` package doc comment per RESEARCH.md Open Question 1 resolution).
</output>

---

## Handoff Notes

**For the executor:**
- All three tasks have copy-paste-ready code in `02-RESEARCH.md`. Resist the urge to "improve" the parser, rename the const, or broaden `isValidWord` — every choice is defended in the pitfalls list (lines 322–402) and a deviation risks regressing a tested invariant.
- The most likely failure mode if you skip the research is Pitfall 4 (trailing newline → 1297 lines → init panic) or Pitfall 1 (too-strict charset → `yo-yo` panic). Both surface as init panics on the very first `go test` run, so the TDD loop catches them fast.
- `go mod tidy` is required after Task 2's first `require`-import; don't skip it or the build breaks.

**For Phase 4 (store) planning:**
- `Allocate` is ready to consume. Signature: `idgen.Allocate(counter uint64) string`. The store is responsible for maintaining the monotonic counter in `state.json` and never decrementing on purge (per CORE-07 wording). Import path: `github.com/tkr41850-debug/mcp-chain/internal/idgen`.
- If Phase 4 planning wants `wordlistSize` exported for log messages ("you've used X of 1296 IDs"), promote the const to `WordlistSize` — trivial non-breaking change per Open Question 2 resolution.

**For Phase 10 (docs/polish) planning:**
- **Add a follow-up task:** ship a top-level `NOTICE` or `THIRD_PARTY_LICENSES.md` citing the EFF short wordlist under CC-BY 4.0 alongside the main project `LICENSE`. Phase 2 placed the attribution only in the `wordlist.go` package doc comment as the minimal compliant position; the NOTICE file is the production-release expectation per RESEARCH.md Open Question 1. Include the upstream URL (`https://www.eff.org/files/2016/09/08/eff_short_wordlist_1.txt`) and the sha256 pin for audit.

**Scope non-goals explicitly NOT done here (intentional, per CONTEXT.md):**
- No randomness / seeding (store owns the counter — Phase 4).
- No collision handling (counter is monotonic — store's job).
- No concurrency primitives (`idgen` is stateless by design).
- No top-level `NOTICE` file (Phase 10 — flagged above).
